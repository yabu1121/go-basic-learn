package main

import "fmt"

/*
【08_pointers.go の練習問題】
以下の3つの問題を解いてください。
*/

// ========== 問題1 ==========
/*
【問題1】値渡しとポインタ渡しの違い
値渡しとポインタ渡しの動作の違いを確認するプログラムを作成してください。

【要件】
1. 整数値を2倍にする関数を2つ作成：
   - doubleValue(n int): 値渡し
   - doublePointer(n *int): ポインタ渡し
2. 同じ変数に対して両方の関数を呼び出す
3. 各関数呼び出し前後の値を出力
4. アドレスも表示して、同じメモリを参照していることを確認

【出力例】
========== 値渡し vs ポインタ渡し ==========
初期値: 10 (アドレス: 0xc000012345)

【値渡しの場合】
関数呼び出し前: 10
関数内で2倍にしました
関数呼び出し後: 10 (変化なし)

【ポインタ渡しの場合】
関数呼び出し前: 10 (アドレス: 0xc000012345)
関数内で2倍にしました
関数呼び出し後: 20 (変化あり、アドレス: 0xc000012345)

【学習ポイント】
- 値渡しとポインタ渡しの違い
- &演算子（アドレス取得）
- *演算子（間接参照）
- メモリアドレスの確認
*/

// ここに関数を定義してください
// func doubleValue(n int) { ... }
// func doublePointer(n *int) { ... }

func problem1() {
	// ここにコードを書いてください

}

// ========== 問題2 ==========
/*
【問題2】構造体とポインタ
構造体を使った銀行口座システムを作成してください。

【要件】
1. Account構造体を定義（名前、残高）
2. 以下の関数を実装：
   - deposit(account *Account, amount int): 入金
   - withdraw(account *Account, amount int) bool: 出金（残高不足ならfalse）
   - transfer(from *Account, to *Account, amount int) bool: 送金
3. 複数の口座を作成して操作
4. 各操作後の残高を表示

【出力例】
========== 銀行口座システム ==========
【初期状態】
太郎の口座: 10000円
花子の口座: 5000円

【操作1: 太郎が3000円入金】
太郎の口座: 13000円

【操作2: 花子が2000円出金】
花子の口座: 3000円

【操作3: 太郎から花子へ5000円送金】
送金成功
太郎の口座: 8000円
花子の口座: 8000円

【操作4: 花子が10000円出金を試みる】
出金失敗: 残高不足
花子の口座: 8000円 (変化なし)

【学習ポイント】
- 構造体の定義
- 構造体へのポインタ
- ポインタを使った値の変更
- 実用的なポインタの使い方
*/

// ここに構造体と関数を定義してください
// type Account struct { ... }
// func deposit(account *Account, amount int) { ... }
// func withdraw(account *Account, amount int) bool { ... }
// func transfer(from *Account, to *Account, amount int) bool { ... }

func problem2() {
	// ここにコードを書いてください

}

// ========== 問題3 ==========
/*
【問題3】スワップ関数の実装
2つの変数の値を入れ替える関数を実装してください。

【要件】
1. swap関数を実装（ポインタを使用）
2. 整数、文字列、構造体で動作確認
3. スワップ前後の値とアドレスを表示
4. 値渡しでスワップを試みた場合との比較も実装

【出力例】
========== スワップ関数 ==========

【整数のスワップ】
スワップ前: a=10 (0xc000012345), b=20 (0xc000012348)
スワップ後: a=20 (0xc000012345), b=10 (0xc000012348)

【文字列のスワップ】
スワップ前: x="Hello" (0xc000056789), y="World" (0xc000056790)
スワップ後: x="World" (0xc000056789), y="Hello" (0xc000056790)

【構造体のスワップ】
スワップ前:
  p1: {Name:太郎 Age:25}
  p2: {Name:花子 Age:23}
スワップ後:
  p1: {Name:花子 Age:23}
  p2: {Name:太郎 Age:25}

【値渡しでの試み（失敗例）】
スワップ前: a=100, b=200
値渡しswap呼び出し
スワップ後: a=100, b=200 (変化なし！)

【学習ポイント】
- ポインタを使った値の交換
- 汎用的なスワップの実装
- 値渡しとポインタ渡しの実践的な違い
- アドレスは変わらず、値だけが変わることの確認
*/

// ここに関数を定義してください
// func swap(a *int, b *int) { ... }
// func swapString(a *string, b *string) { ... }
// func swapPerson(a *Person, b *Person) { ... }
// func swapValue(a int, b int) { ... } // 失敗例

func problem3() {
	// ここにコードを書いてください

}

func main() {
	fmt.Println("========== 問題1 ==========")
	problem1()

	fmt.Println("\n========== 問題2 ==========")
	problem2()

	fmt.Println("\n========== 問題3 ==========")
	problem3()
}

/*
【実行方法】
go run 08.go

【学習ポイント】
1. ポインタの基本（&と*演算子）
2. 値渡しとポインタ渡しの違い
3. 構造体とポインタ
4. ポインタを使った値の変更
5. nilポインタのチェック
6. 実用的なポインタの使い方
7. メモリアドレスの理解

【重要な概念】
- ポインタは変数のメモリアドレスを格納する
- &演算子でアドレスを取得
- *演算子でポインタが指す値を取得・変更
- 関数で元の値を変更したい場合はポインタを使う
- スライスとマップは参照型なのでポインタ不要
*/
